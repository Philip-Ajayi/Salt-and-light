{
  "name": "registration-app",
  "version": "1.0.0",
  "description": "A registration page for Salt and Light Conference using MongoDB native driver",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongodb": "^5.0.0",  // Ensure this is the latest version compatible with your Node.js version
    "body-parser": "^1.19.0"
  }
}
